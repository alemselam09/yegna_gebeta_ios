name: Build iOS IPA for Yegna Gebeta

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install BeeWare
        run: |
          python -m pip install --upgrade pip
          pip install briefcase

      - name: Create iOS project
        run: briefcase create iOS

      - name: Build iOS app
        run: briefcase build iOS

      # (አማራጭ) የiOS አቃፊን ይዘት ለማየት ጊዜያዊ እርምጃ
      - name: List iOS directory contents (debug)
        run: ls -la iOS/

      # የ .ipa ፋይሉን ለማግኘት ትክክለኛው መንገድ
      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: yegna-gebeta-ipa
          path: iOS/**/*.ipa   # በማንኛውም ንዑስ አቃፊ ውስጥ ያለ .ipa ፋይልን ይፈልጋል
